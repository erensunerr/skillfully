# 2026-04-25 Memory

- Added a first-run onboarding modal for authenticated users with no skills. It matches the supplied monochrome reference with import/create choices, close support, a subdued dashboard background, and a bottom guide rail.
- Kept GitHub integration local-only for now: `Connect GitHub` enters a short connecting state and then explains that GitHub import is coming later.
- Wired `Create skill` from the modal into the existing manual skill creation form, and added an empty-dashboard control to reopen the onboarding choice modal.
- Added a minimal `/docs` guide page so the modal's `Read the guide` interaction reaches a real route instead of a 404.
- Added render coverage for the onboarding modal and guide page, plus a pure helper test for showing onboarding by default only when no skills exist.
- Updated the `npm test` script to discover both `.test.ts` and `.test.tsx` files, so the existing landing page test and the new page/modal render tests run by default.
- Copied `app/.env` and `app/.env.local` into this workspace from sibling Skillfully workspaces; both sibling sources had identical hashes and provide InstantDB plus PostHog settings.
- Updated Next to `16.2.4` with `npm install next@latest`; Next rewrote `tsconfig.json` for React automatic JSX and `.next/dev/types`.
- Added non-production local preview fallbacks for Instant React/Admin initialization so missing envs do not crash local browser validation or test runs.
- Validated onboarding through Chrome Computer Use in local preview mode: modal initial state, GitHub unavailable state, create-skill flow, skill detail/snippet state, sign-out reset, and guide navigation all worked.
- Reworked `/docs` into a Demand Curve-inspired multi-step guide with a black hero, rounded step rail, long-form sections, desktop table of contents, mobile wrapping, and a next-up handoff to the blog.
- Added a separate publishable blog surface with `/blog`, static `/blog/[slug]` articles, typed article data in `app/src/content/blog.ts`, and seeded articles for skill authoring and skill quality measurement.
- Added render coverage for the new guide, blog index, and blog article routes. Production validation confirmed `/docs`, `/blog`, and both seeded article routes return 200 and render correctly on desktop and mobile.
