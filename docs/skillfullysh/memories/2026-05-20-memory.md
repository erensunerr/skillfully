# 2026-05-20 Memory

- Fixed GitHub import discovery so malformed YAML frontmatter in one `SKILL.md` becomes a per-skill disabled candidate instead of failing the whole session.
- The import modal now labels malformed frontmatter rows as `Format error`, displays the reason in red, and leaves valid candidates from the same discovery importable.
