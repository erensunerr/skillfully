# 2026-05-19 Memory

- Fixed the GitHub App callback so it resolves the Skillfully owner from the signed install `state` instead of requiring dashboard auth headers on GitHub's browser redirect.
- Kept `/api/github/install` as the authenticated start point; the callback still treats GitHub as a connected repository installation, not an identity provider.
- Added dashboard messaging for GitHub redirect statuses that do not include a usable `github_import` session, including the old `github=unauthorized` case.
- Updated the import start flow so users with an existing stored GitHub installation get a new Skillfully import session instead of being sent back to GitHub's installation management page.
- Added connected repository names to the import modal and changed the repository-management action to `Add repositories`, which explicitly goes to GitHub only when the user wants to update installation repository access.
- Fixed imported-skill editor loading so the markdown editor remounts when the loaded file id changes, and only Markdown/MDX files appear in the editable list; imported JSON/CSV/text files stay available as assets.
- Centralized Agent Skills frontmatter validation so human and agent-authored `SKILL.md` edits must keep explicit required `name` and `description` fields, the package name must match the skill directory, and publish paths reject invalid packages with a spec-linked error.
- Added a publish-confirmation loading state in the dashboard editor modal, prevented duplicate publish clicks, and surfaced publish errors inside the modal.
- Stopped dashboard skill-tab changes from remounting the App Router page by syncing the selected tab URL with native history after local state updates; cached the MDX editor client component to avoid the editor loading flash when returning to the editor tab.
- Fixed the dashboard sidebar `Open Guide` action so it links to `/guide/start-with-agent-skills` in a new tab.
- Updated dashboard and agent publishing so GitHub-managed skills expose a `merge_github_pull_request` next action, require PR merge before showing installation instructions, and only mark post-install confirmation from real install or feedback endpoint usage events.
- Split publishing ownership explicitly: Skillfully-managed skills now publish through the `skillfully` adapter and install from Skillfully public manifest/file URLs, while GitHub-imported skills keep the GitHub PR merge-first flow.
