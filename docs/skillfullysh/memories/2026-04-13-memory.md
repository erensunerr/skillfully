# 2026-04-13 Memory

- Replaced the initial brutalist Skillfully landing page with an editorial monochrome design; the approved copy and layout are now captured in `docs/skillfullysh/specs/2026-04-13-landing-page-refresh-design.md`.
- Kept navigation targets aligned with the existing app routes (`/dashboard`, `/guide`) instead of the placeholder `#` links in the raw reference.
- Added a lightweight page render test so future landing-page copy regressions are easier to catch.
- Fixed the dynamic feedback route handler signature so `npm run build` succeeds under Next.js 15.
- Fixed a dashboard view-state bug where existing skills in the sidebar could still render the main-panel empty state because the default `"list"` screen only showed detail for `"detail"` mode.
- Added explicit pointer cursors for interactive controls globally and removed the dashboard header guide button.
- Added a `prebuild` cleanup for `.next` so production builds do not reuse stale Turbopack dev artifacts.
- Synced the local Instant schema to the configured app with `npx instant-cli push schema --yes`; the remote app was still missing the `feedback` namespace and still had a stale `todos` namespace, which caused feedback writes to fail validation.
