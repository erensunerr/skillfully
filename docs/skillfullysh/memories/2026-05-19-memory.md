# 2026-05-19 Memory

- Implemented private skill sharing and collaboration: email-based `use` and `edit` grants, shared access resolution, dashboard and agent sharing APIs, Resend invite notifications, and authoring access for editors.
- Switched published skill release numbering from semver-style strings to ordinal integer versions while keeping the existing `version` field compatible and adding indexed `versionNumber`.
- Added unified `/api/skills/[skillId]/manifest`, `/files/[...path]`, and `/install` routes. Public skills serve anonymously; private skills require bearer auth and return `404` for authenticated callers without access.
- Updated dashboard shared-skill UX: shared markers in the selector and overview, use-only skills limited to overview/install surfaces, and owner/editor share controls in the editor panel.
- Updated `skills/skillfully/SKILL.md` so connected agents can list visible skills, resolve exact `skill_id` values, grant `use` or `edit` access by email, and revoke non-owner grants.
- Pushed Instant schema and permissions for sharing entities and `skillVersions.versionNumber`; full `npm run lint`, `npm test`, `npm run build`, and `git diff --check` passed.
