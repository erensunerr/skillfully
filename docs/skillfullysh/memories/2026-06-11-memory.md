# 2026-06-11 Memory

- Added a development-only A/B test overlay for local variant switching. Future A/B tests must be registered in `app/src/lib/ab-test-registry.ts` so they appear in the overlay before or alongside launch.
- Removed manual landing query overrides from the Next.js proxy. Landing experiment variants are assigned automatically on `/`; local testing uses the dev overlay instead.
- Renamed the landing experiment request hook from the deprecated `middleware.ts` convention to `proxy.ts` for Next.js 16.
- Removed the `/agent-first` app route surface. The agent-first landing experience remains as a root-page variant, not a separate route.
- Updated the agent-first quiz so answering `No` to knowing what an agent skill is no longer links to the guide. It now shows the short playbooks/SOPs explanation and advances to the next question.
