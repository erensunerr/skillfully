# 2026-05-19 Memory

- Fixed the GitHub App callback so it resolves the Skillfully owner from the signed install `state` instead of requiring dashboard auth headers on GitHub's browser redirect.
- Kept `/api/github/install` as the authenticated start point; the callback still treats GitHub as a connected repository installation, not an identity provider.
- Added dashboard messaging for GitHub redirect statuses that do not include a usable `github_import` session, including the old `github=unauthorized` case.
- Updated the import start flow so users with an existing stored GitHub installation get a new Skillfully import session instead of being sent back to GitHub's installation management page.
- Added connected repository names to the import modal and changed the repository-management action to `Add repositories`, which explicitly goes to GitHub only when the user wants to update installation repository access.
