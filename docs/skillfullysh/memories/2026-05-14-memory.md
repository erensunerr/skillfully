# 2026-05-14 Memory

- Hardened GitHub import ahead of merge: installation repository pagination now stops after 50 pages with a user-facing warning, discovered candidates and warnings are cached on the import session, and import session ids use crypto randomness instead of `Math.random`.
- Added compensating cleanup for failed GitHub skill imports so a mid-import failure removes the partially-created skill, version, file, and publishing target rows before retry.
- Trimmed small GitHub import cleanup items: imported asset storage paths no longer include candidate ids, long per-candidate import errors are truncated before persistence, and callback update code no longer carries unreachable account fallback branches.
