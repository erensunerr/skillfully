# 2026-05-20 Memory

- Fixed GitHub import discovery so malformed YAML frontmatter in one `SKILL.md` becomes a per-skill disabled candidate instead of failing the whole session.
- The import modal now labels malformed frontmatter rows as `Format error`, displays the reason in red, and leaves valid candidates from the same discovery importable.
- Tightened private publishing so publishing no longer flips a private skill public; private skills publish through Skillfully only and remain visible only to users with `use` or `edit` access.
- Added the dashboard settings public/private visibility control, removed archive/reset analytics controls, enabled owner skill deletion, removed the editor version field, and added markdown file create/delete controls plus markdown table toolbar support.
