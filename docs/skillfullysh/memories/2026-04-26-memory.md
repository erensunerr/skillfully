# 2026-04-26 Memory

- Replaced the Skillfully landing page with the supplied monochrome editorial reference: header guide/blog navigation, split hero, schematic visual, onboarding marquee, product dashboard preview, feature grid, persona cards, feedback-loop steps, FAQ, dark CTA, and footer.
- Kept all public landing actions on current routes: `/dashboard` for sign in and sign up, `/guide` for the skills guide, and `/blog` for articles.
- Added native FAQ disclosure interactions so the plus rows open and close without a client component.
- Updated the landing render test to cover the new hero copy, guide/blog routes, feature sections, FAQ, CTA, and absence of the legacy guide route.
- Validated the redesign with the full test suite, production build, Chrome Computer Use inspection, Playwright desktop/mobile screenshots, FAQ disclosure interaction, and local route checks for `/`, `/guide`, `/blog`, and `/dashboard`.
- Added a maintainer editing guide for guide and blog content, documenting that both surfaces are typed TypeScript content modules rather than MDX.
- Rebuilt the authenticated dashboard UI around the supplied operational dashboard reference: sidebar skill selector, version/status header, editor and installation-prompt actions, metric cards, usage chart, skill-health rows, attention and sentiment panels, recent feedback, publishing targets, and version snapshot.
- Kept the dashboard change UI-only. Existing InstantDB hooks, create-skill transaction, onboarding modal wiring, feedback template fetch, and routes were preserved for the next data-model pass.
- Added UI-only `Editor` and `Analytics` dashboard tabs from the supplied references. The editor tab has local file/frontmatter panels, a markdown canvas, validation/version history, publishing destinations, and an install prompt; the analytics tab has local filters, charts, and a feedback table.
- Wired dashboard tab switching through local React state only. No database, schema, route, auth, onboarding, or feedback-template behavior was changed.
- Added UI-only `Settings` and `Account Settings` dashboard surfaces from the supplied references. Skill settings now show general metadata, source, publishing, tracking, and danger-zone sections; account settings now show profile, preferences, security, data/privacy, and an account menu.
- Replaced the sidebar native skill select with a custom skill selector dropdown and added a create-skill modal. Real skill selection and skill creation continue to use the existing selected-skill state and create-skill transaction, while GitHub import remains an inert affordance for a later integration pass.
- Applied the dashboard feedback pass: removed overview health/attention panels, made the desktop sidebar fixed while main content scrolls, gave each dashboard surface its own route, replaced dashboard native selects with `react-select`, swapped the editor canvas to MDXEditor, kept editor publishing actions visible without page scroll, and added light/dark/system theme support.
- Kept the feedback pass dashboard-only. Blog and guide files were left untouched because another agent was working on those surfaces concurrently.
- Ran an adversarial UI/UX browser pass across public pages and dashboard pages at desktop and mobile sizes. Fixed local PostHog no-token console noise, removed the landing preview's native select, constrained the mobile dashboard sidebar and skill selector, prevented editor side-panel content from colliding with the publish footer, and lazy-loaded MDXEditor without the Next development issue overlay.
- Updated the dashboard authoring UI with Recharts-backed overview and analytics charts, collapsible and scrollable editor files/frontmatter panels, a three-step publish confirmation/install-confirmation modal, and revised publishing destinations for LobeHub Skills, ClawHub, and Hermes Skills Hub. Removed `agentskills.io` from dashboard publishing surfaces.
