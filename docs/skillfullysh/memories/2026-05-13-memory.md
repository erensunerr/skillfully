# 2026-05-13 Memory

- Fixed the `Import from GitHub` control inside the `Create new skill` modal. It now routes to `/api/github/install`, matching the onboarding GitHub import entrypoint.
- Added dashboard render coverage so the create-skill modal exposes the GitHub import action.
- Changed the new-skill `SKILL.md` seed to be frontmatter-only by default: `name` is normalized to the Agent Skills spec slug format, `description` is always present, and the old "When to use" / workflow fallback body is no longer generated.
- Removed the dashboard editor's fallback instructional body so empty skills start as empty editable skills with only required frontmatter.
- Split `SKILL.md` frontmatter from the dashboard markdown editor: the persisted file keeps YAML frontmatter, but the editor receives only the body text, while the right-side Frontmatter form recomposes the hidden YAML before save or publish.
