# 2026-05-29 Memory

- Tightened the GitHub import PR review fixes: discovery validation failures now attach to a skill only when the repository scope matches or the skill name is unique in the candidate list, import failures no longer fall back to skill-name matching without `candidate_id`, and candidates are disabled when existing GitHub imports cannot be verified.
- Simplified the dashboard GitHub import submit headers, documented the post-submit `discoveredCount` meaning, and added regression tests for duplicate skill names across repositories plus the existing-import lookup failure path.
- Fixed the GitHub publishing target IDOR: dashboard target updates now require the requested installation to belong to the skill owner and the requested repository to be recorded under that installation, publish context re-validates stored GitHub targets before exposing them to adapters, and GitHub publish adapters require granted target consent before creating installation tokens.
