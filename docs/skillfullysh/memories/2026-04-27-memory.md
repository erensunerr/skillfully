# 2026-04-27 Memory

- Turned the dashboard publishing direction into an approved backend architecture and implementation plan under `docs/skillfullysh/specs`.
- Extended the InstantDB schema with storage files, skill versions, skill files, publishing targets, publish runs, directory submissions, GitHub installations, GitHub repositories, and imports.
- Added owner-scoped permissions for new Skillfully entities and path-based permissions for Instant Storage files.
- Added backend services for skill slug/path normalization, default `SKILL.md` generation, manifest generation, Skillfully excerpt consent, skill draft creation, file listing/updating, publish context building, and publish result recording.
- Added publishing adapters for GitHub App based publishing and manual directory submission packets for LobeHub, ClawHub, and Hermes.
- Added dashboard API routes for skill creation, skill metadata updates, file list/create/upload/update, target settings, and publish orchestration.
- Added public skill manifest/file routes for published skill installation surfaces.
- Added GitHub App callback and webhook routes, plus a dashboard GitHub import route that scans installed repositories for `SKILL.md` files and imports each one as a separate Skillfully skill with consent pending.
- Updated dashboard creation to use the route-backed skill creation flow outside local preview, and updated publish actions to call the publish API.
- Wired the editor to load draft files from the dashboard file API, save markdown edits through the file update route, upload assets to Instant Storage, and block publish while persisted dashboards have unsaved file edits.
- Updated publish finalization to freeze the published version and immediately open the next editable draft from the published files.
- Updated the onboarding GitHub action to send users to a configured GitHub App install URL instead of hardcoding the import path as unavailable.
- Verified the backend publishing pass with the full node test suite and production build.
- Removed seeded demo data from the dashboard and public landing preview, replacing fake metrics, feedback rows, version labels, repo names, account fallbacks, and preview records with real feedback-derived values or explicit empty states.
