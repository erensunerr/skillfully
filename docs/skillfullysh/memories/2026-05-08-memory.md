# 2026-05-08 Memory

- Designed and implemented the GitHub onboarding import flow for the `14-import-from-github-is-not-working` branch.
- Added server-created GitHub import sessions after the GitHub App callback so the dashboard receives `github_import=<sessionId>` instead of a trusted raw installation id.
- Added GitHub skill discovery for any `SKILL.md` under a path containing a `skills` segment, with strict Agent Skills frontmatter validation and invalid/duplicate candidate states.
- Added selected-skill import that stores every importable file under the skill root with Skillfully paths relative to that root, preserves durable GitHub repository ids, and configures imported skills as GitHub-managed with PR-on-publish behavior.
- Allowed dotfiles inside skill roots so imported skills can preserve files like `.skillfully/config.json` while still rejecting path traversal.
- Added a dashboard import modal over the empty dashboard with the loading copy `Finding skills in your GitHub repositories...`, unchecked candidate rows, disabled invalid/already-imported rows, and no-skills/change-access states.
- Updated settings so imported skills display `Managed in GitHub`, their source repository, original skill path, and `Create pull request on publish`.
- Updated GitHub publish path handling so dot-prefixed source roots like `.agents/skills/code-review` publish back to the original repository path.
