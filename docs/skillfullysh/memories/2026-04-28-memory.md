# 2026-04-28 Memory

- Added first-party usage instrumentation through the InstantDB `skillUsageEvents` entity.
- Recorded public page views, manifest update checks, public file loads, and accepted feedback submissions.
- Kept analytics writes server-owned and non-blocking so public install/update/feedback routes continue to work if analytics storage fails.
- Exposed runtime usage counts and recent events in the dashboard overview, dashboard analytics tab, and author-agent analytics API.
- Added dashboard tests for populated usage charts and runtime event rows.
- Added explicit PostHog funnel events for landing page views, sign-in/sign-up CTA clicks, code entry/paste/submit, first skill creation, GitHub skill import, publishing, feedback receipt, and analytics views.
- Addressed PR #9 review notes for publishing security and editor UX.
- Routed GitHub App installation through an authenticated `/api/github/install` endpoint with signed owner-scoped state, made the callback validate the installation with a GitHub App JWT, preserved webhook ownership, and made webhook signature checks fail closed.
- Changed publish semantics so `manual_ready` directory packets no longer mark a skill published unless a real target reports `published` or `submitted`.
- Tightened skill file persistence by querying updates by `{ ownerId, id }`, rejecting duplicate paths within a version, adding asset deletion, and refreshing Instant Storage URLs when serving public manifests/files.
- Updated the editor to autosave, remove the manual save button, make status read-only, expose asset delete actions, show the Skillfully-owned SKILL.md block as a compact non-editable pill, add markdown toolbar controls, and fix the skill selector layering.
- Added the public install callback endpoint and updated the agent install prompt to tell agents to install from GitHub then call `/api/public/skills/<skillId>/install`.
- Replaced the stale Next 16 `next lint` script with `tsc --noEmit --pretty false` and tightened test typings so the lint/typecheck command passes.
- Verified the review fixes with `npm run lint`, `npm test`, and `npm run build` from `app/`.
