# 2026-04-25 Memory

- Added a first-run onboarding modal for authenticated users with no skills. It matches the supplied monochrome reference with import/create choices, close support, a subdued dashboard background, and a bottom guide rail.
- Kept GitHub integration local-only for now: `Connect GitHub` enters a short connecting state and then explains that GitHub import is coming later.
- Wired `Create skill` from the modal into the existing manual skill creation form, and added an empty-dashboard control to reopen the onboarding choice modal.
- Added a minimal `/docs` guide page so the modal's `Read the guide` interaction reaches a real route instead of a 404.
- Added render coverage for the onboarding modal and guide page, plus a pure helper test for showing onboarding by default only when no skills exist.
- Updated the `npm test` script to discover both `.test.ts` and `.test.tsx` files, so the existing landing page test and the new page/modal render tests run by default.
- Local browser verification of `/dashboard` is blocked without `NEXT_PUBLIC_INSTANT_APP_ID`; Playwright desktop/mobile screenshots were used for the new `/docs` route, and tests/build cover the modal code path.
